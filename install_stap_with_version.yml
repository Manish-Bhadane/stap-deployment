---
- name: Setup Dynamic Target Host
  hosts: localhost
  gather_facts: no
  tasks:
    - name: Add target server dynamically
      add_host:
        name: dynamic_stap_target
        groups: dynamic_targets
        ansible_host: "{{ server_ip | trim }}"
        ansible_user: "{{ server_user }}"
        ansible_password: "{{ server_password }}"
        ansible_ssh_pass: "{{ server_password }}"
        ansible_become_password: "{{ server_password }}"
        ansible_connection: ssh
        ansible_shell_type: sh
        ansible_ssh_common_args: "-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o PreferredAuthentications=password -o PubkeyAuthentication=no -o KbdInteractiveAuthentication=no"
        ansible_ssh_extra_args: "-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o PreferredAuthentications=password -o PubkeyAuthentication=no -o KbdInteractiveAuthentication=no"

- name: Install S-TAP with Shell Installer
  hosts: dynamic_targets
  gather_facts: yes
  become: yes
  become_method: su
  vars:
    collector_ip: "{{ collector_ip }}"
    install_dir: "{{ install_dir | default('/usr/local') }}"
    stap_installer: "{{ stap_installer }}"
    stap_repository: "/var/stap-repository/installers"

  pre_tasks:
    - name: Validate required inputs
      assert:
        that:
          - server_ip is defined
          - server_ip | trim | length > 0
          - server_user is defined
          - server_user | length > 0
          - server_password is defined
          - server_password | length > 0
          - collector_ip is defined
          - collector_ip | trim | length > 0
          - install_dir is defined
          - install_dir | trim | length > 0
          - stap_installer is defined
          - stap_installer | trim | length > 0
        fail_msg: "Required survey fields missing: server_ip, server_user, server_password, collector_ip, install_dir, stap_installer"

  tasks:
    - name: Display selected installer
      debug:
        msg: "Installing S-TAP using {{ stap_installer }}"

    - name: Check if installer exists locally
      stat:
        path: "{{ stap_repository }}/{{ stap_installer }}"
      delegate_to: localhost
      become: false
      register: installer_check

    - name: Fail if installer not found
      fail:
        msg: "ERROR: Installer {{ stap_installer }} not found in {{ stap_repository }}"
      when: not installer_check.stat.exists

    - name: Ensure installation directory exists
      file:
        path: "{{ install_dir }}"
        state: directory
        mode: '0755'

    - name: Copy S-TAP installer to target
      copy:
        src: "{{ stap_repository }}/{{ stap_installer }}"
        dest: "/tmp/{{ stap_installer }}"
        mode: '0755'

    - name: Run S-TAP shell installer
      shell: |
        /tmp/{{ stap_installer }} \
          --collector {{ collector_ip }} \
          --install_dir {{ install_dir }} \
          --accept-license \
          --enable-service \
          --start-service
      args:
        creates: /opt/guardium/guard_stap
      register: install_result

    - name: Display installation result
      debug:
        var: install_result.stdout_lines

    - name: Verify S-TAP service
      systemd:
        name: guard_stap
        state: started
        enabled: yes
      register: service_status

    - name: Display success message
      debug:
        msg: "✅ S-TAP successfully installed using {{ stap_installer }}"
